%this will fail till recusive issue is fixed
w_charge(10).
storage_plant(psh).
c(psh,i1,non_charging).
c(psh,i2,5).
p(psh,op,5).
c(psh,is,full).
expect_c(psh,is,full).
expect_p(psh,op,10).
expect_c(psh,i1,non_charging).
expect_c(psh,i2,5).
